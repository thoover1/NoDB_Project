import React, { Component } from "react";

class EditButton extends Component {
  constructor(props) {
    super(props);

  }
  render() {
    // const { name, position, comments } = this.props;
    return (
      // <div>
      //   <button onClick={() => this.editPlayer}>Edit</button>
      // </div>
    );
  }
}

export default EditButton;
